name: Deploy to VPS
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            cd /apps/enfyra-backend
            git reset --hard HEAD
            git clean -fd
            git pull origin main
            docker build -t enfyra-backend:latest .
            docker save enfyra-backend:latest | microk8s ctr image import -
            pulumi stack select enfyra-backend
            pulumi preview
            pulumi up --yes
